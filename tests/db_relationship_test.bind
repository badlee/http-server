DATABASE sqlite://test_rel.db {
    SCHEMA User {
        FIELD name string
    }

    SCHEMA Profile {
        FIELD user_id User.id [delete=cascade update=cascade]
        FIELD bio string
    }

    SCHEMA Order {
        FIELD user_id User.id [has=many]
        FIELD amount number
    }
}
